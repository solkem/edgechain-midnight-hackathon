{
  "version": 2,
  "name": "edgechain-midnight",
  "buildCommand": "rm -f package.json package-lock.json && cp package.vercel.json package.json && npm install --legacy-peer-deps --no-package-lock && npm run build",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping root install - handled in build script'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cross-Origin-Embedder-Policy",
          "value": "require-corp"
        },
        {
          "key": "Cross-Origin-Opener-Policy",
          "value": "same-origin"
        }
      ]
    }
  ]
}
